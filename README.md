# sound_test

25/5/14 9：57
1.功能添加
	添加参数保存功能保存（IP等数据，声强等级数据）  ADD	
	设置界面										ADD
		//			默认声强频率设置
		//			升五降十测试策略
	**
	添加升五降十策略								MOD
		考虑使用2的倍数进行估计
	考虑添加一个结构显示按键，显示病人听力水平		ADD
	考虑添加一个按钮，可以自定义声强参数			MOD

2.具体修改
	1.添加参数保存功能保存（IP等数据，声强等级数据
		0.ui设计，参数保存，
		a. 添加一个全局setting class，可以随时调用
		b.添加gethostip，
		c.setting 使用json保存，采用动态保存的方式
	2.所有设备继承setting变量。采用自己的局部local setting
		a. 保存声强设置
		b.校准		
	3.修改设备，添加一个looptest模式，实现多次声强设置（限制次数）
	4.修改UI通知函数 添加db adjust 字符，实现db的动态变化
	5.添加local 的弹窗 继承设备控件的setting
	5.在运行时 localsetting 不允许运行
	6.校准界面添加一个进度条

25/5/15 9：57

1.替换setting 变成全局的
	1.校准
		目标freq
		最低1最高 freq
		目标db值
		文件存储地址
	2.测试
		最大db允许
		slavehandle 改一些freq获取方式
		delay 延长范围
2.修改校准模式 添加button触发
3.修改校准模式，使用输入ip的方式

4.测试开关机蓝牙稳定性
5.装好5套，进行测试

其他
1.考虑配一个默认的频率表

25/5/16 16:05

考虑string 选项，转-> 具体选项的功能，可以给全局设置更多稳定性

25/5/22 
1.考虑补充完整参数设置
2.添加删除
3.添加一个感觉，生成随机ID
4.

1.改参数显示方式
	1.加入唯一id 用id绑定对象
	2.允许修改
	3.允许单独运行（右键的方式）
	4.右键允许以下功能（在停止的时候允许修改）
		a.单次允许
		b.升五降十
2.允许升五降十
3.pc端保存dbhl 发送dbspl为命令
4.参数修改
5.list结果 
	原来是 slave com 获取
	slavehandle 获取 ->PlayListUpdata.invoke ->
	ui 更新
	关键：希望可以改为ui发送命令下来
	run -> ui 打包数据 -> test 获取此数据 ->开始运行
	run -> 单行数据运行 -> 开始运行 ->
6.run不同的模式[单次][升五][停止]颜色注意	

1.改好了list 现在taskitem 在初始化时，自动赋值id，当修改此结果时，通过一个get set 承接
	
	2.globe 的setting莫名奇妙改写完成后又被覆盖